# Model metadata

Each model has one public YAML metadata file:

```text
model-metadata/<model_id>.yml
```

The permanent `model_id` is `<team_abbr>-<model_abbr>`. Email addresses are not
required in public metadata; the hub operator keeps operational contact details
separately.
